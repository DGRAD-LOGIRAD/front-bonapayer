# Exécuter lint-staged pour formater et linter les fichiers modifiés
npx lint-staged

# Vérifier que tous les fichiers sont formatés correctement
echo "🔍 Vérification du formatage..."
npm run format:check

# Vérifier que le linting passe
echo "🔍 Vérification du linting..."
npm run lint